<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/playerFragment">
    <fragment
        android:id="@+id/playerFragment"
        android:name="com.example.musicplayer.PlayerFragment"
        android:label="@string/music_player">
        <action
            android:id="@+id/action_playerFragment_to_detailsFragment2"
            app:destination="@id/detailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailsFragment"
        android:name="com.example.musicplayer.DetailsFragment"
        android:label="@string/song_details"
        tools:layout="@layout/fragment_details" />
</navigation>